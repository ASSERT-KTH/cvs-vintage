#**

@author <a href="mailto:jvanzyl@periapt.com">Jason van Zyl</a>
@version $Id: FluxPermissionList.vm,v 1.2 2002/01/04 23:16:58 jmcnally Exp $

*#


$page.setTitle("Index")
$page.setBgColor($ui.bgcolor)

#set ( $headings = ["Permission Name"] )

<table>
  
  #if ($showFinder)
  <tr>
    <td align="left">
      <table>
        <tr>
        <form method="post" action="">
          <td align="left">
            #listBox ($flux.FieldList)
          </td>
          <td align="right">
            #textField("searchField" "*" "20")
          </td>
          <td>
            <font face="$ui.sansSerifFonts">
              $link.setPage("permission,FluxPermissionList.vm").setLabel("find")
            </face>
          </td>
        </form>
        </tr>
      </table>
    </td>
  </tr>
  #end
  
  <tr>
    <td>
      <table cellspacing="$ui.tableCellSpacing" cellpadding="$ui.tableCellPadding">
        <tr>
          #foreach ($heading in $headings)
          #headerCell ($heading)
          #end
        </tr>
        
        #foreach ($permission in $flux.Permissions)
        <tr>
          #entryCell ($permission.Name)
          <td>
            <font face="$ui.sansSerifFonts">
              $link.setPage("permission,FluxPermissionForm.vm").addPathInfo("name",$permission.Name).addQueryData("mode","modify").setLabel("Details") &nbsp;
              $link.setPage("permission,FluxPermissionForm.vm").addPathInfo("name",$permission.Name).addQueryData("mode","delete").setLabel("Remove")
            </font>
          </td>
        </tr>
        #end
      </table>
    </td>
  </tr>
  <tr>
    <td>
      <font face="$ui.sansSerifFonts">
        $link.setPage("permission,FluxPermissionForm.vm").addQueryData("mode","insert").setLabel("Add Permission")
      </font>
    <td>
  </tr>
</table>

#if ($showEmbeddedMenu)
  <hr size="1" noshade>
  #parse ("screens/FluxEmbeddedMenu.vm")
#end

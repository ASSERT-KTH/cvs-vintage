#if ($data.User.hasLoggedIn())
    <div class="focus"><strong>$link.setPage("SelectModule.vm").setLabel("Select module")</strong></div>
#else
    <div><strong>$link.setPage("Login.vm").setLabel("Login")</strong></div>
    <div><strong>$link.setPage("Register.vm").setLabel("Register")</strong></div>
#end

## ARTIFACT TYPES
#if ($scarabR.CurrentModule.NavIssueTypes)
  #set ($issueTypes = $scarabR.CurrentModule.NavIssueTypes)
#end
#if ($issueTypes && $issueTypes.size() > 0)

## once cross module searching is implemented we may want to change this,
## but for now there is no reason to show any of these links, if the
## module does not allow issues (such as the global module)
#if ($scarabR.CurrentModule.allowsIssues())
<div class="focus"><strong>
$link.setPage("SelectArtifactType.vm").setLabel("Issue types")
</strong>
$renderer.render("navigations", "ArtifactTypes.vm")
</div>
#end
#end

## AMINISTRATION
#if ($scarabR.CurrentModule)
$renderer.render("navigations", "Administration.vm")
#end
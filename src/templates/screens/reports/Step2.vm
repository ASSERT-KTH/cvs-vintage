#set ($report = $scarabR.Report)
#set ($intakeReport = $intake.Report.mapTo($report))

<div class="app" id="div2-4-1-step2">
#wizardLinks("reports,Step2.vm" $reportParameters $link)
#subWizardLinks("reports,Step2.vm" $reportParameters $link "$l10n.Step2ALink" "reports,Step2.vm" "$l10n.Step2BLink" "reports,Step2b.vm")

<h3>$l10n.SelectReportDataSet</h3>
<p>$l10n.DefiningSubset</p>
<p>$l10n.DefiningSubsetEg</p>

<p><strong>$l10n.Note</strong> $l10n.DefiningSubsetNote</p>

<form method="get" action="$link.setPage("reports,Step2.vm")">
<input type="hidden" name="action" value="GenerateReport" />

<div class="axial">
 <table border="0" cellspacing="2" cellpadding="3">
  <tr>
   <th width="120">$l10n.Attributes</th>
   <td>
   <select name="$intakeReport.AttributesAndOptionsForGrouping.Key"
                multiple="multiple" size="20">
                #attributeOptionOptionList( $report.AllOptionsForGrouping $report.AttributesAndOptionsForGrouping)
   </select>
   </td>
  </tr>
 </table>
</div>

<div class="functnbar2">
## <input type="submit" value="$l10n.Continue"  name="eventSubmit_doStep2agoto3" />&#160;
  <input type="submit" value="$l10n.Next" name="eventSubmit_doStep2agoto2b" />
</div>

$intake.declareGroups()
</form>
</div>

#set ( $report = $scarabR.Report )

<div class="app" id="div2-4-1-a-output">

<h2>$report.Name</h2>
	
<div>
	<strong>Project:</strong> $report.Module.Name
</div>
<div>
	<strong>Date Generated:</strong> $format.getNow("MM/dd/yyyy hh:mm a")
</div>
<div>
	<strong>Generated by:</strong> $report.GeneratedBy.UserName
</div>

#if ($report.Description)
    <p>$report.Description</p>
#end
		
<p>Report Date/Time: $report.NewDate</p>
		
<table width="100%" border="1" cellspacing="2" cellpadding="3">
	<tr>
		<th>&nbsp;</th>
#if ($report.OptionGroups)
    #set ($secondCriteria = $report.OptionGroups)
#elseif ($report.SelectedOptionsForGrouping)
    #set ($secondCriteria = $report.SelectedOptionsForGrouping)
#end

#foreach ( $option in $report.SelectedAxis2Options )
    #if ($secondCriteria)
        #foreach ( $optionGroup in $secondCriteria )
    		<th>$option.DisplayValue $optionGroup.DisplayValue:</th>
        #end
    #else
		<th>$option.DisplayValue:</th>
    #end
#end
    </tr>

#foreach ( $option1 in $report.SelectedAxis1Options )
    #rows()
		<td><strong>$option1.DisplayValue</strong></td>
    #foreach ( $option2 in $report.SelectedAxis2Options )
        #if ($secondCriteria)
            #foreach ( $optionGroup in $secondCriteria )
    		<td>
                $report.getIssueCount($option1.AttributeOption, $option2.AttributeOption, $optionGroup, $report.NewDate )
            </td>
            #end
        #else
    		<td>FIXME</td>
        #end
    #end
    </tr>
#end
		
</table>
		
<p>page 1 of 1</p>

#set ( $action = $link.setPage("Links.vm").setAction("GenerateReport") )
<form method="get" action="$action">

<p class="functnbar">
    <input type="submit" value="save this report" 
        name="eventSubmit_doSavereport" />&nbsp;
    <input type="submit" value="print" name="eventSubmit_doPrint" />&nbsp;
    <input type="submit" value="done" name="eventSubmit_doCancel" />
</p>

#addReportHiddenInputs($report "reports,Report_1.vm")
$intake.declareGroups()
</form>

</div>



package org.tigris.scarab.baseom.peer;

// JDK classes
import java.util.*;

// Village classes
import com.workingdogs.village.*;

// Turbine classes
import org.apache.turbine.om.peer.*;
import org.apache.turbine.util.*;
import org.apache.turbine.util.db.*;
import org.apache.turbine.util.db.map.*;
import org.apache.turbine.util.db.pool.DBConnection;

import org.tigris.scarab.baseom.ScarabAttributeType;

// Local classes
import org.tigris.scarab.baseom.map.*;
import org.tigris.scarab.baseom.*;

/** This class was autogenerated by GenerateMapBuilder on: Mon Jan 08 11:17:17 PST 2001 */
public class ScarabAttributeTypePeer extends BasePeer
{
    /** the mapbuilder for this class */
    private static final ScarabAttributeTypeMapBuilder mapBuilder = 
        (ScarabAttributeTypeMapBuilder)getMapBuilder(ScarabAttributeTypeMapBuilder.CLASS_NAME);

    /** the table name for this class */
    public static final String TABLE_NAME = mapBuilder.getTable();

    /** the column name for the ATTRIBUTE_TYPE_ID field */
    public static final String ATTRIBUTE_TYPE_ID = mapBuilder.getScarabAttributeType_AttributeTypeId();
    /** the column name for the ATTRIBUTE_CLASS_ID field */
    public static final String ATTRIBUTE_CLASS_ID = mapBuilder.getScarabAttributeType_ClassId();
    /** the column name for the ATTRIBUTE_TYPE_NAME field */
    public static final String ATTRIBUTE_TYPE_NAME = mapBuilder.getScarabAttributeType_Name();
    /** the column name for the JAVA_CLASS_NAME field */
    public static final String JAVA_CLASS_NAME = mapBuilder.getScarabAttributeType_JavaClassName();

    /** number of columns for this peer */
    public static final int numColumns =  4;;

    /** Method to do inserts */
    public static Object doInsert( Criteria criteria ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
                                                                     return BasePeer.doInsert( criteria );
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Object doInsert( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
                                                                     return BasePeer.doInsert( criteria, dbCon );
    }

    /** Add all the columns needed to create a new object */
    public static void addSelectColumns (Criteria criteria) throws Exception
    {
            criteria.addSelectColumn( ATTRIBUTE_TYPE_ID );
            criteria.addSelectColumn( ATTRIBUTE_CLASS_ID );
            criteria.addSelectColumn( ATTRIBUTE_TYPE_NAME );
            criteria.addSelectColumn( JAVA_CLASS_NAME );
        }


    /** Create a new object of type cls from a resultset row starting
      * from a specified offset.  This is done so that you can select
      * other rows than just those needed for this object.  You may
      * for example want to create two objects from the same row.
      */
    public static ScarabAttributeType row2Object (Record row, int offset, Class cls ) throws Exception
    {
        ScarabAttributeType obj = (ScarabAttributeType)cls.newInstance();
                                        obj.setAttributeTypeId(row.getValue(offset+0).asInt());
                                            obj.setClassId(row.getValue(offset+1).asInt());
                                            obj.setName(row.getValue(offset+2).asString());
                                            obj.setJavaClassName(row.getValue(offset+3).asString());
                                        obj.setModified(false);
            obj.setNew(false);
                return obj;
    }

    /** Method to do selects */
    public static Vector doSelect( Criteria criteria ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabAttributeType", null);
    }

    /** 
     * Method to do selects.  This method is to be used during a transaction,
     * otherwise use the doSelect(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Vector doSelect( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabAttributeType", dbCon);
    }

    /** Method to do selects. The returned vector will have object
      * of className
      */
    public static Vector doSelect( Criteria criteria, String className, DBConnection dbCon) throws Exception
    {
        addSelectColumns ( criteria );

                                                             
        // BasePeer returns a Vector of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        Vector rows = null;
        if (dbCon == null)
        {
            rows = BasePeer.doSelect(criteria);
        }
        else
        {
            rows = BasePeer.doSelect(criteria, dbCon);
        }
        Vector results = new Vector();

        // populate the object(s)
        for ( int i=0; i<rows.size(); i++ )
        {
            Record row = (Record)rows.elementAt(i);
            results.add (row2Object (row,1,Class.forName (className)));
         }
         return results;
    }

    /**
     * Method to do updates. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        Criteria selectCriteria = new Criteria(2);
                                selectCriteria.put( ATTRIBUTE_TYPE_ID, criteria.remove(ATTRIBUTE_TYPE_ID) );
                                                                                            BasePeer.doUpdate( selectCriteria, criteria );
    }

    /** 
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria, DBConnection dbCon) throws Exception
    {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
         Criteria selectCriteria = new Criteria(2);
                                selectCriteria.put( ATTRIBUTE_TYPE_ID, criteria.remove(ATTRIBUTE_TYPE_ID) );
                                                                                            BasePeer.doUpdate( selectCriteria, criteria, dbCon );
     }

    /** 
     * Method to do deletes.
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
                                                                      BasePeer.doDelete ( criteria );
     }

    /** 
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria, DBConnection dbCon) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
                                                                      BasePeer.doDelete ( criteria, dbCon );
     }

    /** Method to do inserts */
    public static void doInsert( ScarabAttributeType obj ) throws Exception
    {
        obj.setPrimaryKey(doInsert(buildCriteria(obj)));
    }

    /**
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabAttributeType obj) throws Exception
    {
        doUpdate(buildCriteria(obj));
    }
    /**
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabAttributeType obj) throws Exception
    {
        doDelete(buildCriteria(obj));
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(ScarabAttributeType) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to insert into the database.
     */
    public static void doInsert( ScarabAttributeType obj, DBConnection dbCon) throws Exception
    {
        obj.setPrimaryKey(doInsert(buildCriteria(obj), dbCon));
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(ScarabAttributeType) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabAttributeType obj, DBConnection dbCon) throws Exception
    {
        doUpdate(buildCriteria(obj), dbCon);
    }
    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(ScarabAttributeType) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabAttributeType obj, DBConnection dbCon) throws Exception
    {
        doDelete(buildCriteria(obj), dbCon);
    }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( ScarabAttributeType obj )
    {
        Criteria criteria = new Criteria();
                            if ( !obj.isNew() )
            criteria.add( ATTRIBUTE_TYPE_ID, obj.getAttributeTypeId() );
                                        criteria.add( ATTRIBUTE_CLASS_ID, obj.getClassId() );
                                        criteria.add( ATTRIBUTE_TYPE_NAME, obj.getName() );
                                        criteria.add( JAVA_CLASS_NAME, obj.getJavaClassName() );
                            return criteria;
    }

    /** 
     * Retrieve a single object by pk where multiple PK's are separated
     * by colons
     *
     * @param int attribute_type_id
     */
    public static ScarabAttributeType retrieveById(Object pkid) 
        throws Exception
    {
        StringTokenizer stok = new StringTokenizer((String)pkid, ":");
        if ( stok.countTokens() < 1 )
        {   
            throw new TurbineException(
                "id tokens did not match number of primary keys" );
        }
           int attribute_type_id = Integer.parseInt(stok.nextToken());;

       return retrieveByPK(
             attribute_type_id
              );
    }

    /** 
     * Retrieve a single object by pk
     *
     * @param int attribute_type_id
     */
    public static ScarabAttributeType retrieveByPK(
                      int attribute_type_id
                                                                 ) throws Exception
    {
        Criteria criteria = new Criteria();
                       if( attribute_type_id > 0 )
                  criteria.add( ScarabAttributeTypePeer.ATTRIBUTE_TYPE_ID, attribute_type_id );
                                            Vector ScarabAttributeTypeVector = doSelect(criteria);
        if (ScarabAttributeTypeVector.size() != 1)
        {
            throw new Exception("Failed to select one and only one row.");
        }
        else
        {
            return (ScarabAttributeType) ScarabAttributeTypeVector.firstElement();
        }
    }


     
        
                       
     
          


   /**
    * selects a collection of ScarabAttributeType objects pre-filled with their
    * ScarabAttributeClass objects.
    */
    public static Vector doSelectJoinScarabAttributeClass(Criteria c)
        throws Exception
    {
        addSelectColumns(c);
        int offset = numColumns + 1;
        ScarabAttributeClassPeer.addSelectColumns(c);

                                                                     
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabAttributeType obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabAttributeType") );
            ScarabAttributeClass obj2 = ScarabAttributeClassPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset, Class.forName("org.tigris.scarab.baseom.ScarabAttributeClass") );
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabAttributeType temp_obj1 = (ScarabAttributeType)results.elementAt(j);
                ScarabAttributeClass temp_obj2 = temp_obj1.getScarabAttributeClass();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabAttributeTypes(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabAttributeTypes();
                obj2.addScarabAttributeTypes(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
    

  


    
    /** 
     * Retrieve objects by fk
     *
     * @param int attribute_class_id
     */
//    public static Vector retrieveByClassId(int attribute_class_id)
//    {
        
      
}









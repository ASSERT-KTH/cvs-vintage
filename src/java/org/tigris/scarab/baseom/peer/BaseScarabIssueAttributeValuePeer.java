package org.tigris.scarab.baseom.peer;

// JDK classes
import java.util.*;
import java.math.*;

// Village classes
import com.workingdogs.village.*;

// Turbine classes
import org.apache.turbine.om.peer.*;
import org.apache.turbine.util.*;
import org.apache.turbine.util.db.*;
import org.apache.turbine.util.db.map.*;
import org.apache.turbine.util.db.pool.DBConnection;

import org.tigris.scarab.baseom.ScarabIssueAttributeValue;

// Local classes
import org.tigris.scarab.baseom.map.*;
import org.tigris.scarab.baseom.*;

/** This class was autogenerated by GenerateMapBuilder on: Wed Feb 07 17:08:09 PST 2001 */
public abstract class BaseScarabIssueAttributeValuePeer extends BasePeer
{
    /** the mapbuilder for this class */
    private static final ScarabIssueAttributeValueMapBuilder mapBuilder = 
        (ScarabIssueAttributeValueMapBuilder)getMapBuilder(ScarabIssueAttributeValueMapBuilder.CLASS_NAME);

    /** the table name for this class */
    public static final String TABLE_NAME = mapBuilder.getTable();

    /** the column name for the ISSUE_ID field */
    public static final String ISSUE_ID = mapBuilder.getScarabIssueAttributeValue_IssueId();
    /** the column name for the ATTRIBUTE_ID field */
    public static final String ATTRIBUTE_ID = mapBuilder.getScarabIssueAttributeValue_AttributeId();
    /** the column name for the OPTION_ID field */
    public static final String OPTION_ID = mapBuilder.getScarabIssueAttributeValue_OptionId();
    /** the column name for the USER_ID field */
    public static final String USER_ID = mapBuilder.getScarabIssueAttributeValue_UserId();
    /** the column name for the VALUE field */
    public static final String VALUE = mapBuilder.getScarabIssueAttributeValue_Value();
    /** the column name for the DELETED field */
    public static final String DELETED = mapBuilder.getScarabIssueAttributeValue_Deleted();

    /** number of columns for this peer */
    public static final int numColumns =  6;;

    /** Method to do inserts */
    public static Object doInsert( Criteria criteria ) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                            // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                      return BasePeer.doInsert( criteria );
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Object doInsert( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                            // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                      return BasePeer.doInsert( criteria, dbCon );
    }

    /** Add all the columns needed to create a new object */
    public static void addSelectColumns (Criteria criteria) throws Exception
    {
            criteria.addSelectColumn( ISSUE_ID );
            criteria.addSelectColumn( ATTRIBUTE_ID );
            criteria.addSelectColumn( OPTION_ID );
            criteria.addSelectColumn( USER_ID );
            criteria.addSelectColumn( VALUE );
            criteria.addSelectColumn( DELETED );
        }


    /** Create a new object of type cls from a resultset row starting
      * from a specified offset.  This is done so that you can select
      * other rows than just those needed for this object.  You may
      * for example want to create two objects from the same row.
      */
    public static ScarabIssueAttributeValue row2Object (Record row, int offset, Class cls ) throws Exception
    {
        ScarabIssueAttributeValue obj = (ScarabIssueAttributeValue)cls.newInstance();
                                        obj.setIssueId(row.getValue(offset+0).asInt());
                                            obj.setAttributeId(row.getValue(offset+1).asInt());
                                            obj.setOptionId(row.getValue(offset+2).asInt());
                                            obj.setUserId(row.getValue(offset+3).asInt());
                                            obj.setValue(row.getValue(offset+4).asString());
                                            obj.setDeleted
                (1 == row.getValue(offset+5).asInt());
                                        obj.setModified(false);
                obj.setNew(false);
        return obj;
    }

    /** Method to do selects */
    public static Vector doSelect( Criteria criteria ) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabIssueAttributeValue", null);
    }

    /** 
     * Method to do selects.  This method is to be used during a transaction,
     * otherwise use the doSelect(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Vector doSelect( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabIssueAttributeValue", dbCon);
    }

    /** Method to do selects. The returned vector will have object
      * of className
      */
    public static Vector doSelect( Criteria criteria, String className, DBConnection dbCon) throws Exception
    {
        addSelectColumns ( criteria );

                                                                                            // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
              
        // BasePeer returns a Vector of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        Vector rows = null;
        if (dbCon == null)
        {
            rows = BasePeer.doSelect(criteria);
        }
        else
        {
            rows = BasePeer.doSelect(criteria, dbCon);
        }
        Vector results = new Vector();

        // populate the object(s)
        for ( int i=0; i<rows.size(); i++ )
        {
            Record row = (Record)rows.elementAt(i);
            results.add (row2Object (row,1,Class.forName (className)));
         }
         return results;
    }

    /**
     * Method to do updates. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        Criteria selectCriteria = new
            Criteria(mapBuilder.getDatabaseMap().getName(), 2);
                                selectCriteria.put( ISSUE_ID, criteria.remove(ISSUE_ID) );
                                         selectCriteria.put( ATTRIBUTE_ID, criteria.remove(ATTRIBUTE_ID) );
                                                                                                    // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) ) 
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                                BasePeer.doUpdate( selectCriteria, criteria );
    }

    /** 
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria, DBConnection dbCon) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        Criteria selectCriteria = new
            Criteria(mapBuilder.getDatabaseMap().getName(), 2);
                                selectCriteria.put( ISSUE_ID, criteria.remove(ISSUE_ID) );
                                         selectCriteria.put( ATTRIBUTE_ID, criteria.remove(ATTRIBUTE_ID) );
                                                                                                    // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                                BasePeer.doUpdate( selectCriteria, criteria, dbCon );
     }

    /** 
     * Method to do deletes.
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria) throws Exception
     {
         criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                            // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                       BasePeer.doDelete ( criteria );
     }

    /** 
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria, DBConnection dbCon) throws Exception
     {
         criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                            // check for conversion from boolean to int
        if ( criteria.containsKey(DELETED) )
        {
            Object possibleBoolean = criteria.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    criteria.add(DELETED, 1);
                }
                else
                {   
                    criteria.add(DELETED, 0);
                }
            }                     
         }
                       BasePeer.doDelete ( criteria, dbCon );
     }

    /** Method to do inserts */
    public static void doInsert( ScarabIssueAttributeValue obj ) throws Exception
    {
                doInsert(buildCriteria(obj));
                obj.setNew(false);
    }

    /**
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabIssueAttributeValue obj) throws Exception
    {
        doUpdate(buildCriteria(obj));
    }
    /**
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabIssueAttributeValue obj) throws Exception
    {
        doDelete(buildCriteria(obj));
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(ScarabIssueAttributeValue) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to insert into the database.
     */
    public static void doInsert( ScarabIssueAttributeValue obj, DBConnection dbCon) throws Exception
    {
                doInsert(buildCriteria(obj), dbCon);
                obj.setNew(false);
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(ScarabIssueAttributeValue) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabIssueAttributeValue obj, DBConnection dbCon) throws Exception
    {
        doUpdate(buildCriteria(obj), dbCon);
    }
    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(ScarabIssueAttributeValue) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabIssueAttributeValue obj, DBConnection dbCon) throws Exception
    {
        doDelete(buildCriteria(obj), dbCon);
    }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( ScarabIssueAttributeValue obj )
    {
        Criteria criteria = new Criteria();
                                criteria.add( ISSUE_ID, obj.getIssueId() );
                                criteria.add( ATTRIBUTE_ID, obj.getAttributeId() );
                                criteria.add( OPTION_ID, obj.getOptionId() );
                                criteria.add( USER_ID, obj.getUserId() );
                                criteria.add( VALUE, obj.getValue() );
                                criteria.add( DELETED, obj.getDeleted() );
                return criteria;
    }

    /** 
     * Retrieve a single object by pk where multiple PK's are separated
     * by colons
     *
     * @param int issue_id
     * @param int attribute_id
     */
    public static ScarabIssueAttributeValue retrieveById(Object pkid) 
        throws Exception
    {
        StringTokenizer stok = new StringTokenizer((String)pkid, ":");
        if ( stok.countTokens() < 2 )
        {   
            throw new TurbineException(
                "id tokens did not match number of primary keys" );
        }
           int issue_id = Integer.parseInt(stok.nextToken());;
           int attribute_id = Integer.parseInt(stok.nextToken());;

       return retrieveByPK(
             issue_id
              , attribute_id
              );
    }

    /** 
     * Retrieve a single object by pk
     *
     * @param int issue_id
     * @param int attribute_id
     */
    public static ScarabIssueAttributeValue retrieveByPK(
                      int issue_id
                                      , int attribute_id
                                                                             ) throws Exception
    {
        Criteria criteria = new Criteria();
                       if( issue_id > 0 )
                  criteria.add( ScarabIssueAttributeValuePeer.ISSUE_ID, issue_id );
                            if( attribute_id > 0 )
                  criteria.add( ScarabIssueAttributeValuePeer.ATTRIBUTE_ID, attribute_id );
                                                     Vector ScarabIssueAttributeValueVector = doSelect(criteria);
        if (ScarabIssueAttributeValueVector.size() != 1)
        {
            throw new Exception("Failed to select one and only one row.");
        }
        else
        {
            return (ScarabIssueAttributeValue) ScarabIssueAttributeValueVector.firstElement();
        }
    }


        
        
                       
     
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with their
    * ScarabIssue objects.
    */
    public static Vector doSelectJoinScarabIssue(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        ScarabIssuePeer.addSelectColumns(c);

                                                                                            // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
                      
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
            ScarabIssue obj2 = ScarabIssuePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset, Class.forName("org.tigris.scarab.baseom.ScarabIssue") );
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabIssue temp_obj2 = temp_obj1.getScarabIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
         
                       
     
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with their
    * ScarabAttribute objects.
    */
    public static Vector doSelectJoinScarabAttribute(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        ScarabAttributePeer.addSelectColumns(c);

                                                                                            // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
                      
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
            ScarabAttribute obj2 = ScarabAttributePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset, Class.forName("org.tigris.scarab.baseom.ScarabAttribute") );
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttribute temp_obj2 = temp_obj1.getScarabAttribute();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
         
                       
     
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with their
    * ScarabAttributeOption objects.
    */
    public static Vector doSelectJoinScarabAttributeOption(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        ScarabAttributeOptionPeer.addSelectColumns(c);

                                                                                            // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
                      
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
            ScarabAttributeOption obj2 = ScarabAttributeOptionPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset, Class.forName("org.tigris.scarab.baseom.ScarabAttributeOption") );
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttributeOption temp_obj2 = temp_obj1.getScarabAttributeOption();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
         
                       
     
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with their
    * TurbineUser objects.
    */
    public static Vector doSelectJoinTurbineUser(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        TurbineUserPeer.addSelectColumns(c);

                                                                                            // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
                      
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
            TurbineUser obj2 = TurbineUserPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset, Class.forName("org.tigris.scarab.baseom.TurbineUser") );
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                TurbineUser temp_obj2 = temp_obj1.getTurbineUser();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
    

  
  
     
                       
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptScarabIssue(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                                      
                ScarabAttributePeer.addSelectColumns(c);
        int offset3 = offset2 + ScarabAttributePeer.numColumns;
                                          
                ScarabAttributeOptionPeer.addSelectColumns(c);
        int offset4 = offset3 + ScarabAttributeOptionPeer.numColumns;
                                          
                TurbineUserPeer.addSelectColumns(c);
        int offset5 = offset4 + TurbineUserPeer.numColumns;
                                                                                                                          // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
              
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
                    
                                  
                                                
                        
                        ScarabAttribute obj2 = ScarabAttributePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset2, Class.forName("org.tigris.scarab.baseom.ScarabAttribute") );

            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttribute temp_obj2 = temp_obj1.getScarabAttribute();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        ScarabAttributeOption obj3 = ScarabAttributeOptionPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset3, Class.forName("org.tigris.scarab.baseom.ScarabAttributeOption") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttributeOption temp_obj3 = temp_obj1.getScarabAttributeOption();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initScarabIssueAttributeValues();
                obj3.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        TurbineUser obj4 = TurbineUserPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset4, Class.forName("org.tigris.scarab.baseom.TurbineUser") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                TurbineUser temp_obj4 = temp_obj1.getTurbineUser();
                if ( temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj4.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initScarabIssueAttributeValues();
                obj4.addScarabIssueAttributeValues(obj1);
            }
                        results.add(obj1);

        }

        return results;
    }
 
     
                       
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptScarabAttribute(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ScarabIssuePeer.addSelectColumns(c);
        int offset3 = offset2 + ScarabIssuePeer.numColumns;
                                          
                                      
                ScarabAttributeOptionPeer.addSelectColumns(c);
        int offset4 = offset3 + ScarabAttributeOptionPeer.numColumns;
                                          
                TurbineUserPeer.addSelectColumns(c);
        int offset5 = offset4 + TurbineUserPeer.numColumns;
                                                                                                                          // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
              
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
                    
                                                
                        
                        ScarabIssue obj2 = ScarabIssuePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset2, Class.forName("org.tigris.scarab.baseom.ScarabIssue") );

            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabIssue temp_obj2 = temp_obj1.getScarabIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
                        
                                  
                                                
                        
                        ScarabAttributeOption obj3 = ScarabAttributeOptionPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset3, Class.forName("org.tigris.scarab.baseom.ScarabAttributeOption") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttributeOption temp_obj3 = temp_obj1.getScarabAttributeOption();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initScarabIssueAttributeValues();
                obj3.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        TurbineUser obj4 = TurbineUserPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset4, Class.forName("org.tigris.scarab.baseom.TurbineUser") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                TurbineUser temp_obj4 = temp_obj1.getTurbineUser();
                if ( temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj4.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initScarabIssueAttributeValues();
                obj4.addScarabIssueAttributeValues(obj1);
            }
                        results.add(obj1);

        }

        return results;
    }
 
     
                       
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptScarabAttributeOption(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ScarabIssuePeer.addSelectColumns(c);
        int offset3 = offset2 + ScarabIssuePeer.numColumns;
                                          
                ScarabAttributePeer.addSelectColumns(c);
        int offset4 = offset3 + ScarabAttributePeer.numColumns;
                                          
                                      
                TurbineUserPeer.addSelectColumns(c);
        int offset5 = offset4 + TurbineUserPeer.numColumns;
                                                                                                                          // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
              
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
                    
                                                
                        
                        ScarabIssue obj2 = ScarabIssuePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset2, Class.forName("org.tigris.scarab.baseom.ScarabIssue") );

            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabIssue temp_obj2 = temp_obj1.getScarabIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        ScarabAttribute obj3 = ScarabAttributePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset3, Class.forName("org.tigris.scarab.baseom.ScarabAttribute") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttribute temp_obj3 = temp_obj1.getScarabAttribute();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initScarabIssueAttributeValues();
                obj3.addScarabIssueAttributeValues(obj1);
            }
                        
                                  
                                                
                        
                        TurbineUser obj4 = TurbineUserPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset4, Class.forName("org.tigris.scarab.baseom.TurbineUser") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                TurbineUser temp_obj4 = temp_obj1.getTurbineUser();
                if ( temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj4.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initScarabIssueAttributeValues();
                obj4.addScarabIssueAttributeValues(obj1);
            }
                        results.add(obj1);

        }

        return results;
    }
 
     
                       
          


   /**
    * selects a collection of ScarabIssueAttributeValue objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptTurbineUser(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                ScarabIssuePeer.addSelectColumns(c);
        int offset3 = offset2 + ScarabIssuePeer.numColumns;
                                          
                ScarabAttributePeer.addSelectColumns(c);
        int offset4 = offset3 + ScarabAttributePeer.numColumns;
                                          
                ScarabAttributeOptionPeer.addSelectColumns(c);
        int offset5 = offset4 + ScarabAttributeOptionPeer.numColumns;
                                          
                                                                                                                      // check for conversion from boolean to int
        if ( c.containsKey(DELETED) )
        {
            Object possibleBoolean = c.get(DELETED);
            if ( possibleBoolean instanceof Boolean )
            {
                if ( ((Boolean)possibleBoolean).booleanValue() )
                {
                    c.add(DELETED, 1);
                }
                else
                {   
                    c.add(DELETED, 0);
                }
            }                     
         }
              
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            ScarabIssueAttributeValue obj1 = 
                row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            1, Class.forName("org.tigris.scarab.baseom.ScarabIssueAttributeValue") );
                    
                                                
                        
                        ScarabIssue obj2 = ScarabIssuePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset2, Class.forName("org.tigris.scarab.baseom.ScarabIssue") );

            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabIssue temp_obj2 = temp_obj1.getScarabIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initScarabIssueAttributeValues();
                obj2.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        ScarabAttribute obj3 = ScarabAttributePeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset3, Class.forName("org.tigris.scarab.baseom.ScarabAttribute") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttribute temp_obj3 = temp_obj1.getScarabAttribute();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initScarabIssueAttributeValues();
                obj3.addScarabIssueAttributeValues(obj1);
            }
                        
                                                
                        
                        ScarabAttributeOption obj4 = ScarabAttributeOptionPeer
                .row2Object((com.workingdogs.village.Record)rows.elementAt(i),
                            offset4, Class.forName("org.tigris.scarab.baseom.ScarabAttributeOption") );

            
             newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                ScarabIssueAttributeValue temp_obj1 = (ScarabIssueAttributeValue)results.elementAt(j);
                ScarabAttributeOption temp_obj4 = temp_obj1.getScarabAttributeOption();
                if ( temp_obj4.getPrimaryKey().equals(obj4.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj4.addScarabIssueAttributeValues(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj4.initScarabIssueAttributeValues();
                obj4.addScarabIssueAttributeValues(obj1);
            }
                        
                                  results.add(obj1);

        }

        return results;
    }
  


  
    /** 
     * Retrieve objects by fk
     *
     * @param int issue_id
     */
//    public static Vector retrieveByIssueId(int issue_id)
//    {
        
    
    /** 
     * Retrieve objects by fk
     *
     * @param int attribute_id
     */
//    public static Vector retrieveByAttributeId(int attribute_id)
//    {
        
    
    /** 
     * Retrieve objects by fk
     *
     * @param int option_id
     */
//    public static Vector retrieveByOptionId(int option_id)
//    {
        
    
    /** 
     * Retrieve objects by fk
     *
     * @param int user_id
     */
//    public static Vector retrieveByUserId(int user_id)
//    {
        
      
}









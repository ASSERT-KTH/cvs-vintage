package org.tigris.scarab.baseom.peer;

// JDK classes
import java.util.*;

// Village classes
import com.workingdogs.village.*;

// Turbine classes
import org.apache.turbine.om.peer.*;
import org.apache.turbine.util.*;
import org.apache.turbine.util.db.*;
import org.apache.turbine.util.db.map.*;
import org.apache.turbine.util.db.pool.DBConnection;

import org.tigris.scarab.baseom.ScarabRModuleVisitorRole;

// Local classes
import org.tigris.scarab.baseom.map.*;
import org.tigris.scarab.baseom.*;

/** This class was autogenerated by GenerateMapBuilder on: Fri Dec 15 13:47:21 PST 2000 */
public class ScarabRModuleVisitorRolePeer extends BasePeer
{
    /** the mapbuilder for this class */
    private static final ScarabRModuleVisitorRoleMapBuilder mapBuilder = 
        (ScarabRModuleVisitorRoleMapBuilder)getMapBuilder(ScarabRModuleVisitorRoleMapBuilder.CLASS_NAME);

    /** the table name for this class */
    public static final String TABLE_NAME = mapBuilder.getTable();

    /** the column name for the MODULE_ID field */
    public static final String MODULE_ID = mapBuilder.getScarabRModuleVisitorRole_ModuleId();
    /** the column name for the VISITOR_ID field */
    public static final String VISITOR_ID = mapBuilder.getScarabRModuleVisitorRole_VisitorId();
    /** the column name for the ROLE_ID field */
    public static final String ROLE_ID = mapBuilder.getScarabRModuleVisitorRole_RoleId();
    /** the column name for the DELETED field */
    public static final String DELETED = mapBuilder.getScarabRModuleVisitorRole_Deleted();

    /** number of columns for this peer */
    public static final int numColumns =  4;;

    /** Method to do inserts */
    public static Object doInsert( Criteria criteria ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return BasePeer.doInsert( criteria );
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Object doInsert( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return BasePeer.doInsert( criteria, dbCon );
    }

    /** Add all the columns needed to create a new object */
    public static void addSelectColumns (Criteria criteria) throws Exception
    {
            criteria.addSelectColumn( MODULE_ID );
            criteria.addSelectColumn( VISITOR_ID );
            criteria.addSelectColumn( ROLE_ID );
            criteria.addSelectColumn( DELETED );
        }


    /** Create a new object of type cls from a resultset row starting
      * from a specified offset.  This is done so that you can select
      * other rows than just those needed for this object.  You may
      * for example want to create two objects from the same row.
      */
    public static ScarabRModuleVisitorRole row2Object (Record row, int offset, Class cls ) throws Exception
    {
        ScarabRModuleVisitorRole obj = (ScarabRModuleVisitorRole)cls.newInstance();
                            obj.setModuleId (row.getValue(offset+0).asInt());
                                obj.setVisitorId (row.getValue(offset+1).asInt());
                                obj.setRoleId (row.getValue(offset+2).asInt());
                                obj.setDeleted (row.getValue(offset+3).asString());
                                        obj.setModified(false);
            obj.setNew(false);
                return obj;
    }

    /** Method to do selects */
    public static Vector doSelect( Criteria criteria ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabRModuleVisitorRole", null);
    }

    /** 
     * Method to do selects.  This method is to be used during a transaction,
     * otherwise use the doSelect(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static Vector doSelect( Criteria criteria, DBConnection dbCon ) throws Exception
    {
        criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
        return doSelect (criteria,"org.tigris.scarab.baseom.ScarabRModuleVisitorRole", dbCon);
    }

    /** Method to do selects. The returned vector will have object
      * of className
      */
    public static Vector doSelect( Criteria criteria, String className, DBConnection dbCon) throws Exception
    {
        addSelectColumns ( criteria );

        // BasePeer returns a Vector of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        Vector rows = null;
        if (dbCon == null)
        {
            rows = BasePeer.doSelect(criteria);
        }
        else
        {
            rows = BasePeer.doSelect(criteria, dbCon);
        }
        Vector results = new Vector();

        // populate the object(s)
        for ( int i=0; i<rows.size(); i++ )
        {
            Record row = (Record)rows.elementAt(i);
            results.add (row2Object (row,1,Class.forName (className)));
         }
         return results;
    }

    /**
     * Method to do updates. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
     public static void doUpdate(Criteria criteria) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
         Criteria selectCriteria = new Criteria(2);
                       selectCriteria.put( MODULE_ID, criteria.remove(MODULE_ID) );
                                selectCriteria.put( VISITOR_ID, criteria.remove(VISITOR_ID) );
                                selectCriteria.put( ROLE_ID, criteria.remove(ROLE_ID) );
                                     BasePeer.doUpdate( selectCriteria, criteria );
     }
    /**
    /** 
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
     public static void doUpdate(Criteria criteria, DBConnection dbCon) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
         Criteria selectCriteria = new Criteria(2);
                       selectCriteria.put( MODULE_ID, criteria.remove(MODULE_ID) );
                                selectCriteria.put( VISITOR_ID, criteria.remove(VISITOR_ID) );
                                selectCriteria.put( ROLE_ID, criteria.remove(ROLE_ID) );
                                     BasePeer.doUpdate( selectCriteria, criteria, dbCon );
     }

    /** 
     * Method to do deletes.
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
         BasePeer.doDelete ( criteria );
     }

    /** 
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria, DBConnection dbCon) throws Exception
     {
         criteria.setDbName(getMapBuilder().getDatabaseMap().getName());
         BasePeer.doDelete ( criteria, dbCon );
     }

    /** Method to do inserts */
    public static void doInsert( ScarabRModuleVisitorRole obj ) throws Exception
    {
        obj.setId(doInsert(buildCriteria(obj)));
    }

    /**
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabRModuleVisitorRole obj) throws Exception
    {
        doUpdate(buildCriteria(obj));
    }
    /**
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabRModuleVisitorRole obj) throws Exception
    {
        doDelete(buildCriteria(obj));
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(ScarabRModuleVisitorRole) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to insert into the database.
     */
    public static void doInsert( ScarabRModuleVisitorRole obj, DBConnection dbCon) throws Exception
    {
        obj.setId(doInsert(buildCriteria(obj), dbCon));
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(ScarabRModuleVisitorRole) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(ScarabRModuleVisitorRole obj, DBConnection dbCon) throws Exception
    {
        doUpdate(buildCriteria(obj), dbCon);
    }
    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(ScarabRModuleVisitorRole) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(ScarabRModuleVisitorRole obj, DBConnection dbCon) throws Exception
    {
        doDelete(buildCriteria(obj), dbCon);
    }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( ScarabRModuleVisitorRole obj )
    {
        Criteria criteria = new Criteria();
                        if( obj.getModuleId() > 0 )
            criteria.add( ScarabRModuleVisitorRolePeer.MODULE_ID, obj.getModuleId() );
                                if( obj.getVisitorId() > 0 )
            criteria.add( ScarabRModuleVisitorRolePeer.VISITOR_ID, obj.getVisitorId() );
                                if( obj.getRoleId() > 0 )
            criteria.add( ScarabRModuleVisitorRolePeer.ROLE_ID, obj.getRoleId() );
                                criteria.add( DELETED, obj.getDeleted() );
                        return criteria;
    }

    /** 
     * Retrieve a single object by pk
     *
     * @param int module_id
     * @param int visitor_id
     * @param int role_id
     */
    public static ScarabRModuleVisitorRole retrieveByPK(
                      int module_id
                                      , int visitor_id
                                      , int role_id
                                         ) throws Exception
    {
        Criteria criteria = new Criteria();
                       if( module_id > 0 )
                  criteria.add( ScarabRModuleVisitorRolePeer.MODULE_ID, module_id );
                            if( visitor_id > 0 )
                  criteria.add( ScarabRModuleVisitorRolePeer.VISITOR_ID, visitor_id );
                            if( role_id > 0 )
                  criteria.add( ScarabRModuleVisitorRolePeer.ROLE_ID, role_id );
                          Vector ScarabRModuleVisitorRoleVector = doSelect(criteria);
        if (ScarabRModuleVisitorRoleVector.size() != 1)
        {
            throw new Exception("Failed to select one and only one row.");
        }
        else
        {
            return (ScarabRModuleVisitorRole) ScarabRModuleVisitorRoleVector.firstElement();
        }
    }

  
    /** 
     * Retrieve objects by fk
     *
     * @param int module_id
     */
//    public static Vector retrieveByModuleId(int module_id)
//    {
        
    
    /** 
     * Retrieve objects by fk
     *
     * @param int visitor_id
     */
//    public static Vector retrieveByVisitorId(int visitor_id)
//    {
        
      
}






package org.tigris.scarab.baseom;

// JDK classes
import java.util.*;

// Turbine classes
import org.apache.turbine.om.BaseObject;
import org.apache.turbine.om.peer.BasePeer;
import org.tigris.scarab.baseom.peer.*;
import org.apache.turbine.util.db.Criteria;
import org.apache.turbine.util.ObjectUtils;
import org.apache.turbine.util.Log;
import org.apache.turbine.util.db.pool.DBConnection;

/** This class was autogenerated by GenerateMapBuilder on: Fri Dec 15 13:47:21 PST 2000 */
public class ScarabModule extends BaseObject
{
    /** the value for the module_id field */
    private int module_id;
    /** the value for the module_name field */
    private String module_name;
    /** the value for the module_description field */
    private String module_description;
    /** the value for the module_url field */
    private String module_url;
    /** the value for the parent_id field */
    private int parent_id;
    /** the value for the owner_id field */
    private int owner_id;
    /** the value for the qa_contact_id field */
    private int qa_contact_id;
    /** the value for the deleted field */
    private String deleted;


    /**
     * Get the ModuleId
     * @return int
     */
     public int getModuleId()
     {
          return module_id;
     }

                            
    /**
     * Set the value of ModuleId
     */
     public void setModuleId(int v ) throws Exception
     {
  
  
       
          // update associated ScarabIssue
          if (collScarabIssues != null )
          {
              for (int i=0; i<collScarabIssues.size(); i++)
              {
                  ((ScarabIssue)collScarabIssues.elementAt(i))
                      .setModuleId(v);
              }
          }

 
       
          // update associated ScarabRModuleAttribute
          if (collScarabRModuleAttributes != null )
          {
              for (int i=0; i<collScarabRModuleAttributes.size(); i++)
              {
                  ((ScarabRModuleAttribute)collScarabRModuleAttributes.elementAt(i))
                      .setModuleId(v);
              }
          }

 
       
          // update associated ScarabRModuleVisitor
          if (collScarabRModuleVisitors != null )
          {
              for (int i=0; i<collScarabRModuleVisitors.size(); i++)
              {
                  ((ScarabRModuleVisitor)collScarabRModuleVisitors.elementAt(i))
                      .setModuleId(v);
              }
          }

 
           if (this.module_id != v)
           {
              this.module_id = v;
              setModified(true);
          }
     }
    /**
     * Get the Name
     * @return String
     */
     public String getName()
     {
          return module_name;
     }

        
    /**
     * Set the value of Name
     */
     public void setName(String v ) 
     {
  
  
           if ( !ObjectUtils.equals(this.module_name, v) )
           {
              this.module_name = v;
              setModified(true);
          }
     }
    /**
     * Get the Description
     * @return String
     */
     public String getDescription()
     {
          return module_description;
     }

        
    /**
     * Set the value of Description
     */
     public void setDescription(String v ) 
     {
  
  
           if ( !ObjectUtils.equals(this.module_description, v) )
           {
              this.module_description = v;
              setModified(true);
          }
     }
    /**
     * Get the Url
     * @return String
     */
     public String getUrl()
     {
          return module_url;
     }

        
    /**
     * Set the value of Url
     */
     public void setUrl(String v ) 
     {
  
  
           if ( !ObjectUtils.equals(this.module_url, v) )
           {
              this.module_url = v;
              setModified(true);
          }
     }
    /**
     * Get the ParentId
     * @return int
     */
     public int getParentId()
     {
          return parent_id;
     }

        
    /**
     * Set the value of ParentId
     */
     public void setParentId(int v ) 
     {
  
  
           if (this.parent_id != v)
           {
              this.parent_id = v;
              setModified(true);
          }
     }
    /**
     * Get the OwnerId
     * @return int
     */
     public int getOwnerId()
     {
          return owner_id;
     }

        
    /**
     * Set the value of OwnerId
     */
     public void setOwnerId(int v ) 
     {
  
  
           if (this.owner_id != v)
           {
              this.owner_id = v;
              setModified(true);
          }
     }
    /**
     * Get the QaContactId
     * @return int
     */
     public int getQaContactId()
     {
          return qa_contact_id;
     }

        
    /**
     * Set the value of QaContactId
     */
     public void setQaContactId(int v ) 
     {
  
  
           if (this.qa_contact_id != v)
           {
              this.qa_contact_id = v;
              setModified(true);
          }
     }
    /**
     * Get the Deleted
     * @return String
     */
     public String getDeleted()
     {
          return deleted;
     }

        
    /**
     * Set the value of Deleted
     */
     public void setDeleted(String v ) 
     {
  
  
           if ( !ObjectUtils.equals(this.deleted, v) )
           {
              this.deleted = v;
              setModified(true);
          }
     }

 
    
              
      
    /**
     * Collection to store aggregation of collScarabIssues
     */
    private Vector collScarabIssues;
    /**
     * Temporary storage of collScarabIssues to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
//    private Vector tempcollScarabIssues;

    public void initScarabIssues()
    {
        if (collScarabIssues == null)
            collScarabIssues = new Vector();
    }

    /**
     * Method called to associate a ScarabIssue object to this object
     * through the ScarabIssue foreign key attribute
     *
     * @param ScarabIssue l
     */
    public void addScarabIssues(ScarabIssue l) throws Exception
    {
        /*
        if (collScarabIssues == null)
        {
            if (tempcollScarabIssues == null)
            {
                tempcollScarabIssues = new Vector();
            }
            tempcollScarabIssues.add(l);
        }
        else
        {
            collScarabIssues.add(l);
        }
        */
        getScarabIssues().add(l);
        l.setScarabModule(this);
    }

    /**
     * The criteria used to select the current contents of collScarabIssues
     */
    private Criteria lastScarabIssuesCriteria = null;

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabIssues from storage.
     */
    public Vector getScarabIssues() throws Exception
    {
        return getScarabIssues(new Criteria(5));
    }

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabIssues from storage.
     */
    public Vector getScarabIssues(Criteria criteria) throws Exception
    {
        if (collScarabIssues == null)
        {
            if ( isNew() ) 
            {
               collScarabIssues = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabIssuePeer.MODULE_ID, getModuleId() );               
                   collScarabIssues = ScarabIssuePeer.doSelect(criteria);
            }
/*
            if (tempcollScarabIssues != null)
            {
                for (int i=0; i<tempcollScarabIssues.size(); i++)
                {
                    collScarabIssues.add(tempcollScarabIssues.get(i));
                }
                tempcollScarabIssues = null;
            }
*/
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabIssuePeer.MODULE_ID, getModuleId() );               
               if ( lastScarabIssuesCriteria.size() == criteria.size() )
            {
                newCriteria = false;
                for (Enumeration e=criteria.keys(); e.hasMoreElements(); )
                {
                    Object key = e.nextElement();
                    if ( lastScarabIssuesCriteria.containsKey(key) )
                    {
                        if ( !criteria.get(key).equals(
                            lastScarabIssuesCriteria.get(key)) )
                        {
                            newCriteria = true;
                            break;
                        }
                    }
                    else
                    {
                        newCriteria = true;
                        break;
                    }
                }
            }
            if (newCriteria)
            {
                collScarabIssues = ScarabIssuePeer.doSelect(criteria);  
            }
        }
        lastScarabIssuesCriteria = criteria; 

        return collScarabIssues;
    }

           
      
    /**
     * Collection to store aggregation of collScarabRModuleAttributes
     */
    private Vector collScarabRModuleAttributes;
    /**
     * Temporary storage of collScarabRModuleAttributes to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
//    private Vector tempcollScarabRModuleAttributes;

    public void initScarabRModuleAttributes()
    {
        if (collScarabRModuleAttributes == null)
            collScarabRModuleAttributes = new Vector();
    }

    /**
     * Method called to associate a ScarabRModuleAttribute object to this object
     * through the ScarabRModuleAttribute foreign key attribute
     *
     * @param ScarabRModuleAttribute l
     */
    public void addScarabRModuleAttributes(ScarabRModuleAttribute l) throws Exception
    {
        /*
        if (collScarabRModuleAttributes == null)
        {
            if (tempcollScarabRModuleAttributes == null)
            {
                tempcollScarabRModuleAttributes = new Vector();
            }
            tempcollScarabRModuleAttributes.add(l);
        }
        else
        {
            collScarabRModuleAttributes.add(l);
        }
        */
        getScarabRModuleAttributes().add(l);
        l.setScarabModule(this);
    }

    /**
     * The criteria used to select the current contents of collScarabRModuleAttributes
     */
    private Criteria lastScarabRModuleAttributesCriteria = null;

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabRModuleAttributes from storage.
     */
    public Vector getScarabRModuleAttributes() throws Exception
    {
        return getScarabRModuleAttributes(new Criteria(5));
    }

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabRModuleAttributes from storage.
     */
    public Vector getScarabRModuleAttributes(Criteria criteria) throws Exception
    {
        if (collScarabRModuleAttributes == null)
        {
            if ( isNew() ) 
            {
               collScarabRModuleAttributes = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabRModuleAttributePeer.MODULE_ID, getModuleId() );               
                   collScarabRModuleAttributes = ScarabRModuleAttributePeer.doSelect(criteria);
            }
/*
            if (tempcollScarabRModuleAttributes != null)
            {
                for (int i=0; i<tempcollScarabRModuleAttributes.size(); i++)
                {
                    collScarabRModuleAttributes.add(tempcollScarabRModuleAttributes.get(i));
                }
                tempcollScarabRModuleAttributes = null;
            }
*/
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabRModuleAttributePeer.MODULE_ID, getModuleId() );               
               if ( lastScarabRModuleAttributesCriteria.size() == criteria.size() )
            {
                newCriteria = false;
                for (Enumeration e=criteria.keys(); e.hasMoreElements(); )
                {
                    Object key = e.nextElement();
                    if ( lastScarabRModuleAttributesCriteria.containsKey(key) )
                    {
                        if ( !criteria.get(key).equals(
                            lastScarabRModuleAttributesCriteria.get(key)) )
                        {
                            newCriteria = true;
                            break;
                        }
                    }
                    else
                    {
                        newCriteria = true;
                        break;
                    }
                }
            }
            if (newCriteria)
            {
                collScarabRModuleAttributes = ScarabRModuleAttributePeer.doSelect(criteria);  
            }
        }
        lastScarabRModuleAttributesCriteria = criteria; 

        return collScarabRModuleAttributes;
    }

           
      
    /**
     * Collection to store aggregation of collScarabRModuleVisitors
     */
    private Vector collScarabRModuleVisitors;
    /**
     * Temporary storage of collScarabRModuleVisitors to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
//    private Vector tempcollScarabRModuleVisitors;

    public void initScarabRModuleVisitors()
    {
        if (collScarabRModuleVisitors == null)
            collScarabRModuleVisitors = new Vector();
    }

    /**
     * Method called to associate a ScarabRModuleVisitor object to this object
     * through the ScarabRModuleVisitor foreign key attribute
     *
     * @param ScarabRModuleVisitor l
     */
    public void addScarabRModuleVisitors(ScarabRModuleVisitor l) throws Exception
    {
        /*
        if (collScarabRModuleVisitors == null)
        {
            if (tempcollScarabRModuleVisitors == null)
            {
                tempcollScarabRModuleVisitors = new Vector();
            }
            tempcollScarabRModuleVisitors.add(l);
        }
        else
        {
            collScarabRModuleVisitors.add(l);
        }
        */
        getScarabRModuleVisitors().add(l);
        l.setScarabModule(this);
    }

    /**
     * The criteria used to select the current contents of collScarabRModuleVisitors
     */
    private Criteria lastScarabRModuleVisitorsCriteria = null;

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabRModuleVisitors from storage.
     */
    public Vector getScarabRModuleVisitors() throws Exception
    {
        return getScarabRModuleVisitors(new Criteria(5));
    }

    /**
     * If this collection has already been initialized, it returns
     * the collection. Otherwise if this ScarabModule is new, it will return
     * an empty collection; or if this ScarabModule has previously
     * been saved, it will retrieve related ScarabRModuleVisitors from storage.
     */
    public Vector getScarabRModuleVisitors(Criteria criteria) throws Exception
    {
        if (collScarabRModuleVisitors == null)
        {
            if ( isNew() ) 
            {
               collScarabRModuleVisitors = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabRModuleVisitorPeer.MODULE_ID, getModuleId() );               
                   collScarabRModuleVisitors = ScarabRModuleVisitorPeer.doSelect(criteria);
            }
/*
            if (tempcollScarabRModuleVisitors != null)
            {
                for (int i=0; i<tempcollScarabRModuleVisitors.size(); i++)
                {
                    collScarabRModuleVisitors.add(tempcollScarabRModuleVisitors.get(i));
                }
                tempcollScarabRModuleVisitors = null;
            }
*/
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabRModuleVisitorPeer.MODULE_ID, getModuleId() );               
               if ( lastScarabRModuleVisitorsCriteria.size() == criteria.size() )
            {
                newCriteria = false;
                for (Enumeration e=criteria.keys(); e.hasMoreElements(); )
                {
                    Object key = e.nextElement();
                    if ( lastScarabRModuleVisitorsCriteria.containsKey(key) )
                    {
                        if ( !criteria.get(key).equals(
                            lastScarabRModuleVisitorsCriteria.get(key)) )
                        {
                            newCriteria = true;
                            break;
                        }
                    }
                    else
                    {
                        newCriteria = true;
                        break;
                    }
                }
            }
            if (newCriteria)
            {
                collScarabRModuleVisitors = ScarabRModuleVisitorPeer.doSelect(criteria);  
            }
        }
        lastScarabRModuleVisitorsCriteria = criteria; 

        return collScarabRModuleVisitors;
    }

     
    


    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     */
    public void save() throws Exception
    {
         DBConnection dbCon = null;
        try
        {
            dbCon = BasePeer.beginTransaction(
                ScarabModulePeer.getMapBuilder()
                .getDatabaseMap().getName());
            save(dbCon);
        }
        catch(Exception e)
        {
            BasePeer.rollBackTransaction(dbCon);
            throw e;
        }
        BasePeer.commitTransaction(dbCon);

     }

      // flag to prevent endless save loop, if this object is referenced
    // by another object which falls in this transaction.
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     */
    public void save(DBConnection dbCon) throws Exception
    {
        if (!alreadyInSave)
      {
        alreadyInSave = true;
          if (isModified())
        {
            if (isNew())
            {
                ScarabModulePeer.doInsert(this, dbCon);
            }
            else
            {
                ScarabModulePeer.doUpdate(this, dbCon);
                setNew(false);
            }
        }

             
      
          if (collScarabIssues != null )
          {
              for (int i=0; i<collScarabIssues.size(); i++)
              {
                  ((ScarabIssue)collScarabIssues.elementAt(i)).save(dbCon);
              }
          }
            
      
          if (collScarabRModuleAttributes != null )
          {
              for (int i=0; i<collScarabRModuleAttributes.size(); i++)
              {
                  ((ScarabRModuleAttribute)collScarabRModuleAttributes.elementAt(i)).save(dbCon);
              }
          }
            
      
          if (collScarabRModuleVisitors != null )
          {
              for (int i=0; i<collScarabRModuleVisitors.size(); i++)
              {
                  ((ScarabRModuleVisitor)collScarabRModuleVisitors.elementAt(i)).save(dbCon);
              }
          }
             alreadyInSave = false;
      }
      }

                                                                
    /** 
     * Set the Id using pk values.
     *
     * @param int module_id
     */
    public void setId(
                      int module_id
                                                                                                                 ) throws Exception
    {
                     setModuleId(module_id);
                                                                                                    }


    /** 
     * Set the Id using a : separated String of pk values.
     */
    public void setId(Object id) throws Exception
    {
        StringTokenizer st = new StringTokenizer(id.toString(), ":");
                           setModuleId( Integer.parseInt(st.nextToken()) );
                                                                                                            }


    /** 
     * returns an id that differentiates this object from others
     * of its class.
     */
    public Object getId() 
    {
        return ""
                      + getModuleId()
                                                                                                                 ;
    } 

    /** 
     * returns an id that can be used to specify this object in
     * a query string.
     */
    public String getQueryOID() 
    {
        return "ScarabModule[" + getId() + "]";
    }

    /**
     * Makes a copy of this object.  
     * It creates a new object filling in the simple attributes.
      * It then fills all the association collections and sets the
     * related objects to isNew=true.
      */
    public ScarabModule copy() throws Exception
    {
        ScarabModule copyObj = new ScarabModule();
         copyObj.setModuleId(module_id);
         copyObj.setName(module_name);
         copyObj.setDescription(module_description);
         copyObj.setUrl(module_url);
         copyObj.setParentId(parent_id);
         copyObj.setOwnerId(owner_id);
         copyObj.setQaContactId(qa_contact_id);
         copyObj.setDeleted(deleted);
 
            
      
         Vector v = copyObj.getScarabIssues();
         for (int i=0; i<v.size(); i++)
         {
             ((BaseObject)v.elementAt(i)).setNew(true);
         }

               
      
         v = copyObj.getScarabRModuleAttributes();
         for (int i=0; i<v.size(); i++)
         {
             ((BaseObject)v.elementAt(i)).setNew(true);
         }

               
      
         v = copyObj.getScarabRModuleVisitors();
         for (int i=0; i<v.size(); i++)
         {
             ((BaseObject)v.elementAt(i)).setNew(true);
         }

     
                        
        copyObj.setModuleId(NEW_ID);
                                                            return copyObj;
    }             

}





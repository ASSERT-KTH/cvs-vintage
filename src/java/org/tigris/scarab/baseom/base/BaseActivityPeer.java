package org.tigris.scarab.baseom.base;

// JDK classes
import java.util.*;
import java.math.*;

// Village classes
import com.workingdogs.village.*;

// Turbine classes
import org.apache.turbine.om.*;
import org.apache.turbine.om.peer.*;
import org.apache.turbine.util.*;
import org.apache.turbine.util.db.*;
import org.apache.turbine.util.db.map.*;
import org.apache.turbine.util.db.pool.DBConnection;


// Local classes
import org.tigris.scarab.baseom.map.*;
import org.tigris.scarab.baseom.peer.*;
import org.tigris.scarab.baseom.*;


/**
  * This class was autogenerated by Torque on:
  *
  * [Thu Feb 22 18:38:08 PST 2001]
  *
  */
public abstract class BaseActivityPeer extends BasePeer
{
    /** the mapbuilder for this class */
    private static final ActivityMapBuilder mapBuilder = 
        (ActivityMapBuilder)getMapBuilder(ActivityMapBuilder.CLASS_NAME);

    /** the table name for this class */
    public static final String TABLE_NAME = mapBuilder.getTable();

    /** the column name for the ISSUE_ID field */
    public static final String ISSUE_ID = mapBuilder.getActivity_IssueId();
    /** the column name for the ATTRIBUTE_ID field */
    public static final String ATTRIBUTE_ID = mapBuilder.getActivity_AttributeId();
    /** the column name for the TRANSACTION_ID field */
    public static final String TRANSACTION_ID = mapBuilder.getActivity_TransactionId();
    /** the column name for the OLD_VALUE field */
    public static final String OLD_VALUE = mapBuilder.getActivity_OldValue();
    /** the column name for the NEW_VALUE field */
    public static final String NEW_VALUE = mapBuilder.getActivity_NewValue();

    /** number of columns for this peer */
    public static final int numColumns =  5;

    /** A class that can be returned by this peer. */
    protected static final String CLASSNAME_DEFAULT = 
        "org.tigris.scarab.baseom.Activity";

    /** A class that can be returned by this peer. */
    protected static final Class CLASS_DEFAULT = initClass();

    /** Initialization method for static CLASS_DEFAULT attribute */
    private static Class initClass()
    {
        Class c = null;
        try
        { 
            c = Class.forName(CLASSNAME_DEFAULT);
        }
        catch (Exception e)
        {
            Log.error("A FATAL ERROR has occurred which should not" +
                "have happened under any circumstance.  Please notify" +
                "Turbine and give as many details as possible including the " +
                "error stacktrace.", e);
        }
        return c;
    }


    /** Method to do inserts */
    public static ObjectKey doInsert( Criteria criteria ) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                   return BasePeer.doInsert( criteria );
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Criteria) method.  It will take care of 
     * the connection details internally. 
     */
    public static ObjectKey doInsert( Criteria criteria, DBConnection dbCon ) 
        throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                   return BasePeer.doInsert( criteria, dbCon );
    }

    /** Add all the columns needed to create a new object */
    public static void addSelectColumns (Criteria criteria) throws Exception
    {
            criteria.addSelectColumn( ISSUE_ID );
            criteria.addSelectColumn( ATTRIBUTE_ID );
            criteria.addSelectColumn( TRANSACTION_ID );
            criteria.addSelectColumn( OLD_VALUE );
            criteria.addSelectColumn( NEW_VALUE );
        }


    /** 
     * Create a new object of type cls from a resultset row starting
     * from a specified offset.  This is done so that you can select
     * other rows than just those needed for this object.  You may
     * for example want to create two objects from the same row.
     */
    public static Activity row2Object (Record row, 
                                              int offset, 
                                              Class cls ) 
        throws Exception
    {
        Activity obj = (Activity)cls.newInstance();
                                                        obj.setIssueId(
                new SimpleKey( row.getValue(offset+0).asString() ));
                                                                            obj.setAttributeId(
                new SimpleKey( row.getValue(offset+1).asString() ));
                                                                            obj.setTransactionId(
                new SimpleKey( row.getValue(offset+2).asString() ));
                                                                            obj.setOldValue(row.getValue(offset+3).asString());
                                                                            obj.setNewValue(row.getValue(offset+4).asString());
                                                        obj.setModified(false);
                obj.setNew(false);

        return obj;
    }

    /** Method to do selects */
    public static Vector doSelect( Criteria criteria ) throws Exception
    {
        return populateObjects( doSelectVillageRecords(criteria) ); 
    }


    /** Method to do selects within a transaction */
    public static Vector doSelect( Criteria criteria, 
                                   DBConnection dbCon ) 
        throws Exception
    {
        return populateObjects( doSelectVillageRecords(criteria, dbCon) ); 
    }

    /** 
     * Grabs the raw Village records to be formed into objects.
     * This method handles connections internally 
     */
    public static Vector doSelectVillageRecords( Criteria criteria ) 
        throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        if (criteria.getSelectColumns().size() == 0)
        {
            addSelectColumns ( criteria );
        }

                                                                           
        // BasePeer returns a Vector of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        return BasePeer.doSelect(criteria);
    }


    /** 
     * Grabs the raw Village records to be formed into objects.
     * This method should be used for transactions 
     */
    public static Vector doSelectVillageRecords( Criteria criteria, 
                                                 DBConnection dbCon ) 
        throws Exception
    {
        addSelectColumns ( criteria );

                                                                           
        // BasePeer returns a Vector of Value (Village) arrays.  The array
        // order follows the order columns were placed in the Select clause.
        return BasePeer.doSelect(criteria, dbCon);
    }

    /** 
     * The returned vector will contain objects of the default type or
     * objects that inherit from the default.
     */
    public static Vector populateObjects(Vector records) 
        throws Exception
    {
        Vector results = new Vector(records.size());

        // populate the object(s)
        for ( int i=0; i<records.size(); i++ )
        {
            Record row = (Record)records.elementAt(i);
            results.add(row2Object( row,1, CLASS_DEFAULT ));
        }
        return results;
    }




    /**
     * Method to do updates. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        Criteria selectCriteria = new
            Criteria(mapBuilder.getDatabaseMap().getName(), 2);
                                selectCriteria.put( ISSUE_ID, criteria.remove(ISSUE_ID) );
                                         selectCriteria.put( ATTRIBUTE_ID, criteria.remove(ATTRIBUTE_ID) );
                                         selectCriteria.put( TRANSACTION_ID, criteria.remove(TRANSACTION_ID) );
                                                                     BasePeer.doUpdate( selectCriteria, criteria );
    }

    /** 
     * Method to do updates.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used to create the UPDATE statement.
     */
    public static void doUpdate(Criteria criteria, DBConnection dbCon) throws Exception
    {
        criteria.setDbName(mapBuilder.getDatabaseMap().getName());
        Criteria selectCriteria = new
            Criteria(mapBuilder.getDatabaseMap().getName(), 2);
                                selectCriteria.put( ISSUE_ID, criteria.remove(ISSUE_ID) );
                                         selectCriteria.put( ATTRIBUTE_ID, criteria.remove(ATTRIBUTE_ID) );
                                         selectCriteria.put( TRANSACTION_ID, criteria.remove(TRANSACTION_ID) );
                                                                     BasePeer.doUpdate( selectCriteria, criteria, dbCon );
     }

    /** 
     * Method to do deletes.
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria) throws Exception
     {
         criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                    BasePeer.doDelete ( criteria );
     }

    /** 
     * Method to do deletes.  This method is to be used during a transaction,
     * otherwise use the doDelete(Criteria) method.  It will take care of 
     * the connection details internally. 
     *
     * @param Criteria object containing data that is used DELETE from database.
     */
     public static void doDelete(Criteria criteria, DBConnection dbCon) throws Exception
     {
         criteria.setDbName(mapBuilder.getDatabaseMap().getName());
                                                                                    BasePeer.doDelete ( criteria, dbCon );
     }

    /** Method to do inserts */
    public static void doInsert( Activity obj ) throws Exception
    {
                doInsert(buildCriteria(obj));
                obj.setNew(false);
    }

    /**
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(Activity obj) throws Exception
    {
        doUpdate(buildCriteria(obj));
    }
    /**
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(Activity obj) throws Exception
    {
        doDelete(buildCriteria(obj));
    }

    /** 
     * Method to do inserts.  This method is to be used during a transaction,
     * otherwise use the doInsert(Activity) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to insert into the database.
     */
    public static void doInsert( Activity obj, DBConnection dbCon) throws Exception
    {
                doInsert(buildCriteria(obj), dbCon);
                obj.setNew(false);
    }

    /**
     * Method to do update.  This method is to be used during a transaction,
     * otherwise use the doUpdate(Activity) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to update in the database.
     */
    public static void doUpdate(Activity obj, DBConnection dbCon) throws Exception
    {
        doUpdate(buildCriteria(obj), dbCon);
    }
    /**
     * Method to delete.  This method is to be used during a transaction,
     * otherwise use the doDelete(Activity) method.  It will take 
     * care of the connection details internally. 
     *
     * @param obj the data object to delete in the database.
     */
    public static void doDelete(Activity obj, DBConnection dbCon) throws Exception
    {
        doDelete(buildCriteria(obj), dbCon);
    }

    /** Build a Criteria object from the data object for this peer */
    public static Criteria buildCriteria( Activity obj )
    {
        Criteria criteria = new Criteria();
                                criteria.add( ISSUE_ID, obj.getIssueId() );
                                criteria.add( ATTRIBUTE_ID, obj.getAttributeId() );
                                criteria.add( TRANSACTION_ID, obj.getTransactionId() );
                                criteria.add( OLD_VALUE, obj.getOldValue() );
                                criteria.add( NEW_VALUE, obj.getNewValue() );
                return criteria;
    }

    /** 
     * Retrieve a single object by pk
     *
     * @param ObjectKey pk
     */
    public static Activity retrieveByPK( ObjectKey pk )
        throws Exception
    {
        Criteria criteria = new Criteria();
        SimpleKey[] keys = (SimpleKey[])pk.getInternalObject();
                    criteria.add( ISSUE_ID, keys[0] );
                        criteria.add( ATTRIBUTE_ID, keys[1] );
                        criteria.add( TRANSACTION_ID, keys[2] );
                    Vector v = doSelect(criteria);
        if ( v.size() != 1)
        {
            throw new Exception("Failed to select one and only one row.");
        }
        else
        {
            return (Activity)v.firstElement();
        }
    }


    /** 
     * retrieve object using using pk values.
     *
     * @param SimpleKey issue_id
     * @param SimpleKey attribute_id
     * @param SimpleKey transaction_id
     */
    public static Activity retrieveByPK(
     SimpleKey issue_id
            , SimpleKey attribute_id
            , SimpleKey transaction_id
                ) throws Exception
    {
        Criteria criteria = new Criteria(5);
        criteria.add( ISSUE_ID, issue_id );
        criteria.add( ATTRIBUTE_ID, attribute_id );
        criteria.add( TRANSACTION_ID, transaction_id );
        Vector v = doSelect(criteria);
        if ( v.size() != 1)
        {
            throw new Exception("Failed to select one and only one row.");
        }
        else
        {
            return (Activity) v.firstElement();
        }
    }



       
        
                       
     
          


   /**
    * selects a collection of Activity objects pre-filled with their
    * Issue objects.
    */
    public static Vector doSelectJoinIssue(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        IssuePeer.addSelectColumns(c);

                                                                                   
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);

            Activity obj1 = row2Object( row,1, CLASS_DEFAULT );


            Issue obj2 = IssuePeer
                .row2Object(row, offset, 
                    IssuePeer.CLASS_DEFAULT);
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Issue temp_obj2 = temp_obj1.getIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
         
                       
     
          


   /**
    * selects a collection of Activity objects pre-filled with their
    * Attribute objects.
    */
    public static Vector doSelectJoinAttribute(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        AttributePeer.addSelectColumns(c);

                                                                                   
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);

            Activity obj1 = row2Object( row,1, CLASS_DEFAULT );


            Attribute obj2 = AttributePeer
                .row2Object(row, offset, 
                    AttributePeer.CLASS_DEFAULT);
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Attribute temp_obj2 = temp_obj1.getAttribute();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
         
                       
     
          


   /**
    * selects a collection of Activity objects pre-filled with their
    * Transaction objects.
    */
    public static Vector doSelectJoinTransaction(Criteria c)
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset = numColumns + 1;
        TransactionPeer.addSelectColumns(c);

                                                                                   
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);

            Activity obj1 = row2Object( row,1, CLASS_DEFAULT );


            Transaction obj2 = TransactionPeer
                .row2Object(row, offset, 
                    TransactionPeer.CLASS_DEFAULT);
            
            boolean newObject = true;
            for (int j=0; j<results.size(); j++)
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Transaction temp_obj2 = temp_obj1.getTransaction();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
            results.add(obj1);

        }

        return results;
    }
    

  
  
     
                       
          


   /**
    * selects a collection of Activity objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptIssue(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                                      
                AttributePeer.addSelectColumns(c);
        int offset3 = offset2 + AttributePeer.numColumns;
                                          
                TransactionPeer.addSelectColumns(c);
        int offset4 = offset3 + TransactionPeer.numColumns;
                                                                                                         
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);


                Activity obj1 = row2Object( row,1, CLASS_DEFAULT );
  
                    
                                  
                                                
                        
            

            Attribute obj2 = AttributePeer
                .row2Object(row, offset2, 
                    AttributePeer.CLASS_DEFAULT);
            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Attribute temp_obj2 = temp_obj1.getAttribute();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
                        
                                                
                        
            

            Transaction obj3 = TransactionPeer
                .row2Object(row, offset3, 
                    TransactionPeer.CLASS_DEFAULT);
            
             newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Transaction temp_obj3 = temp_obj1.getTransaction();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initActivitys();
                obj3.addActivitys(obj1);
            }
                        results.add(obj1);

        }

        return results;
    }
 
     
                       
          


   /**
    * selects a collection of Activity objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptAttribute(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                IssuePeer.addSelectColumns(c);
        int offset3 = offset2 + IssuePeer.numColumns;
                                          
                                      
                TransactionPeer.addSelectColumns(c);
        int offset4 = offset3 + TransactionPeer.numColumns;
                                                                                                         
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);


                Activity obj1 = row2Object( row,1, CLASS_DEFAULT );
  
                    
                                                
                        
            

            Issue obj2 = IssuePeer
                .row2Object(row, offset2, 
                    IssuePeer.CLASS_DEFAULT);
            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Issue temp_obj2 = temp_obj1.getIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
                        
                                  
                                                
                        
            

            Transaction obj3 = TransactionPeer
                .row2Object(row, offset3, 
                    TransactionPeer.CLASS_DEFAULT);
            
             newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Transaction temp_obj3 = temp_obj1.getTransaction();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initActivitys();
                obj3.addActivitys(obj1);
            }
                        results.add(obj1);

        }

        return results;
    }
 
     
                       
          


   /**
    * selects a collection of Activity objects pre-filled with 
    * all related objects.
    */
    public static Vector doSelectJoinAllExceptTransaction(Criteria c) 
        throws Exception
    {
         c.setDbName(mapBuilder.getDatabaseMap().getName());

        addSelectColumns(c);
        int offset2 = numColumns + 1;
                    
                IssuePeer.addSelectColumns(c);
        int offset3 = offset2 + IssuePeer.numColumns;
                                          
                AttributePeer.addSelectColumns(c);
        int offset4 = offset3 + AttributePeer.numColumns;
                                          
                                                                                                     
        Vector rows = BasePeer.doSelect(c);
        Vector results = new Vector();

        for (int i=0; i<rows.size(); i++)
        {
            Record row = (Record)rows.elementAt(i);


                Activity obj1 = row2Object( row,1, CLASS_DEFAULT );
  
                    
                                                
                        
            

            Issue obj2 = IssuePeer
                .row2Object(row, offset2, 
                    IssuePeer.CLASS_DEFAULT);
            
             boolean  newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Issue temp_obj2 = temp_obj1.getIssue();
                if ( temp_obj2.getPrimaryKey().equals(obj2.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj2.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj2.initActivitys();
                obj2.addActivitys(obj1);
            }
                        
                                                
                        
            

            Attribute obj3 = AttributePeer
                .row2Object(row, offset3, 
                    AttributePeer.CLASS_DEFAULT);
            
             newObject = true;
            for (int j=0; j<results.size(); j++)    
            {
                Activity temp_obj1 = (Activity)results.elementAt(j);
                Attribute temp_obj3 = temp_obj1.getAttribute();
                if ( temp_obj3.getPrimaryKey().equals(obj3.getPrimaryKey() ) )
                {
                    newObject = false;
                    temp_obj3.addActivitys(obj1);
                    break;
                }
            }
            if (newObject)
            {
                obj3.initActivitys();
                obj3.addActivitys(obj1);
            }
                        
                                  results.add(obj1);

        }

        return results;
    }
  




}


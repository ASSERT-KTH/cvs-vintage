package org.tigris.scarab.baseom;

// JDK classes
import java.util.*;

// Turbine classes
import org.apache.turbine.om.BaseObject;
import org.apache.turbine.om.peer.BasePeer;
import org.tigris.scarab.baseom.peer.*;
import org.apache.turbine.util.db.Criteria;
import org.apache.turbine.util.ObjectUtils;
import org.apache.turbine.util.StringUtils;
import org.apache.turbine.util.ParameterParser;
import org.apache.turbine.util.Log;
import org.apache.turbine.util.db.pool.DBConnection;

/** This class was autogenerated by GenerateMapBuilder on: Mon Jan 08 11:17:17 PST 2001 */
public class ScarabDependType extends BaseObject
{
    /** the value for the depend_type_id field */
    private int depend_type_id;
    /** the value for the depend_type_name field */
    private String depend_type_name;


    /**
     * Get the DependTypeId
     * @return int
     */
     public int getDependTypeId()
     {
          return depend_type_id;
     }

                            
    /**
     * Set the value of DependTypeId
     */
     public void setDependTypeId(int v ) throws Exception
     {
  
       
        
                
          // update associated ScarabDepend
          if (collScarabDepends != null )
          {
              for (int i=0; i<collScarabDepends.size(); i++)
              {
                  ((ScarabDepend)collScarabDepends.elementAt(i))
                      .setTypeId(v);
              }
          }
       

            if (this.depend_type_id != v)
           {
              this.depend_type_id = v;
              setModified(true);
          }
     }
    /**
     * Get the Name
     * @return String
     */
     public String getName()
     {
          return depend_type_name;
     }

        
    /**
     * Set the value of Name
     */
     public void setName(String v ) 
     {
  
  

           if ( !ObjectUtils.equals(this.depend_type_name, v) )

           {
              this.depend_type_name = v;
              setModified(true);
          }
     }

 
    
                
      
    /**
     * Collection to store aggregation of collScarabDepends
     */
    private Vector collScarabDepends;
    /**
     * Temporary storage of collScarabDepends to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
//    private Vector tempcollScarabDepends;

    public void initScarabDepends()
    {
        if (collScarabDepends == null)
            collScarabDepends = new Vector();
    }

    /**
     * Method called to associate a ScarabDepend object to this object
     * through the ScarabDepend foreign key attribute
     *
     * @param ScarabDepend l
     */
    public void addScarabDepends(ScarabDepend l) throws Exception
    {
        /*
        if (collScarabDepends == null)
        {
            if (tempcollScarabDepends == null)
            {
                tempcollScarabDepends = new Vector();
            }
            tempcollScarabDepends.add(l);
        }
        else
        {
            collScarabDepends.add(l);
        }
        */
        getScarabDepends().add(l);
        l.setScarabDependType(this);
    }

    /**
     * The criteria used to select the current contents of collScarabDepends
     */
    private Criteria lastScarabDependsCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of 
     * getScarabDepends(new Criteria())
     */
    public Vector getScarabDepends() throws Exception
    {
        if (collScarabDepends == null)
        {
            collScarabDepends = getScarabDepends(new Criteria(10));
        }
        return collScarabDepends;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection. 
     * Otherwise if this ScarabDependType is new, it will return
     * an empty collection; or if this ScarabDependType has previously
     * been saved, it will retrieve related ScarabDepends from storage.
     */
    public Vector getScarabDepends(Criteria criteria) throws Exception
    {
        if (collScarabDepends == null)
        {
            if ( isNew() ) 
            {
               collScarabDepends = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
                   collScarabDepends = ScarabDependPeer.doSelect(criteria);
            }
/*
            if (tempcollScarabDepends != null)
            {
                for (int i=0; i<tempcollScarabDepends.size(); i++)
                {
                    collScarabDepends.add(tempcollScarabDepends.get(i));
                }
                tempcollScarabDepends = null;
            }
*/
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
               if ( !lastScarabDependsCriteria.equals(criteria)  )
            {
                collScarabDepends = ScarabDependPeer.doSelect(criteria);  
            }
        }
        lastScarabDependsCriteria = criteria; 

        return collScarabDepends;
    }
     

        
      
      
              
                            
            
        
   
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection. 
     * Otherwise if this ScarabDependType is new, it will return
     * an empty collection; or if this ScarabDependType has previously
     * been saved, it will retrieve related ScarabDepends from storage.
     */
    public Vector getScarabDependsJoinScarabIssueRelatedByObservedId(Criteria criteria) 
        throws Exception
    {
        if (collScarabDepends == null)
        {
            if ( isNew() ) 
            {
               collScarabDepends = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
                   collScarabDepends = ScarabDependPeer.doSelectJoinScarabIssueRelatedByObservedId(criteria);
            }
            
            
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
               if ( !lastScarabDependsCriteria.equals(criteria)  )
            {
                collScarabDepends = ScarabDependPeer.doSelectJoinScarabIssueRelatedByObservedId(criteria);
            }
        }
        lastScarabDependsCriteria = criteria; 

        return collScarabDepends;
    }
      
      
      
              
                            
            
        
   
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection. 
     * Otherwise if this ScarabDependType is new, it will return
     * an empty collection; or if this ScarabDependType has previously
     * been saved, it will retrieve related ScarabDepends from storage.
     */
    public Vector getScarabDependsJoinScarabIssueRelatedByObserverId(Criteria criteria) 
        throws Exception
    {
        if (collScarabDepends == null)
        {
            if ( isNew() ) 
            {
               collScarabDepends = new Vector();       
            } 
            else
            {
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
                   collScarabDepends = ScarabDependPeer.doSelectJoinScarabIssueRelatedByObserverId(criteria);
            }
            
            
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            boolean newCriteria = true;
                   criteria.add(ScarabDependPeer.DEPEND_TYPE_ID, getDependTypeId() );               
               if ( !lastScarabDependsCriteria.equals(criteria)  )
            {
                collScarabDepends = ScarabDependPeer.doSelectJoinScarabIssueRelatedByObserverId(criteria);
            }
        }
        lastScarabDependsCriteria = criteria; 

        return collScarabDepends;
    }
      
      
         
          
                    
            
        
      



     
    


    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     */
    public void save() throws Exception
    {
         DBConnection dbCon = null;
        try
        {
            dbCon = BasePeer.beginTransaction(
                ScarabDependTypePeer.getMapBuilder()
                .getDatabaseMap().getName());
            save(dbCon);
        }
        catch(Exception e)
        {
            BasePeer.rollBackTransaction(dbCon);
            throw e;
        }
        BasePeer.commitTransaction(dbCon);

     }

      // flag to prevent endless save loop, if this object is referenced
    // by another object which falls in this transaction.
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     */
    public void save(DBConnection dbCon) throws Exception
    {
        if (!alreadyInSave)
      {
        alreadyInSave = true;
          if (isModified())
        {
            if (isNew())
            {
                ScarabDependTypePeer.doInsert(this, dbCon);
            }
            else
            {
                ScarabDependTypePeer.doUpdate(this, dbCon);
                setNew(false);
            }
        }

                                    
                
          if (collScarabDepends != null )
          {
              for (int i=0; i<collScarabDepends.size(); i++)
              {
                  ((ScarabDepend)collScarabDepends.elementAt(i)).save(dbCon);
              }
          }
                  alreadyInSave = false;
      }
      }

                                        
    /** 
     * Set the Id using pk values.
     *
     * @param int depend_type_id
     */
    public void setPrimaryKey(
                      int depend_type_id
                                         ) throws Exception
    {
                     setDependTypeId(depend_type_id);
                            }


    /** 
     * Set the Id using a : separated String of pk values.
     */
    public void setPrimaryKey(Object id) throws Exception
    {
        StringTokenizer st = new StringTokenizer(id.toString(), ":");
                     setDependTypeId( Integer.parseInt(st.nextToken()) );
                  }


    /** 
     * returns an id that differentiates this object from others
     * of its class.
     */
    public Object getPrimaryKey() 
    {
        return ""
                      + getDependTypeId()
                                         ;
    } 

    /** 
     * returns an id that can be used to specify this object in
     * a query string.
     */
    public String getQueryOID() 
    {
        return "ScarabDependType[" + getPrimaryKey() + "]";
    }

    public static Vector getScarabDependTypes(ParameterParser pp)
        throws Exception
    {
        Vector v = new Vector();
        // look for classname in keys
        Enumeration keys = pp.keys();
        while ( keys.hasMoreElements() )
        {
            String ppKey = (String)keys.nextElement();
            String[] parsedString = StringUtils.parseObjectKey(ppKey);
            if ("ScarabDependType".equalsIgnoreCase(
                     parsedString[StringUtils.PPKEY_CLASSNAME]))
            {
                ScarabDependType obj = null;
                if (parsedString[StringUtils.PPKEY_ID].equals("PK") )
                {
                    obj = new ScarabDependType();
                    v.addElement(obj);
  
                          obj.setDependTypeId(Integer
                        .parseInt(parsedString[StringUtils.PPKEY_ID]));
                        }
                else
                {
  
                          int depend_type_id = Integer
                        .parseInt(parsedString[StringUtils.PPKEY_ID]);
        

              
                    for ( int i=0; i<v.size(); i++) 
                    {
                        ScarabDependType tempObj = 
                            (ScarabDependType)v.get(i);
                        if ( depend_type_id == tempObj.getDependTypeId() ) 
                        {
                            obj = tempObj;
                            break;
                        }
                    }
                    if ( obj == null ) 
                    {
                        obj = new ScarabDependType();
                        obj.setDependTypeId(depend_type_id);
                        v.addElement(obj);
                    }
                }
                String property = parsedString[StringUtils.PPKEY_PROPERTY];

                        if ( property.equalsIgnoreCase("Name") ) 
                {
                    obj.setName(pp.getString(ppKey));
                }
                }
        }
        return v;
    }

    /**
     * Makes a copy of this object.  
     * It creates a new object filling in the simple attributes.
      * It then fills all the association collections and sets the
     * related objects to isNew=true.
      */
    public ScarabDependType copy() throws Exception
    {
        ScarabDependType copyObj = new ScarabDependType();
         copyObj.setDependTypeId(depend_type_id);
         copyObj.setName(depend_type_name);
 
                                  
                
         Vector v = copyObj.getScarabDepends();
         for (int i=0; i<v.size(); i++)
         {
             ((BaseObject)v.elementAt(i)).setNew(true);
         }
         
                        
        copyObj.setDependTypeId(NEW_ID);
                  return copyObj;
    }

}

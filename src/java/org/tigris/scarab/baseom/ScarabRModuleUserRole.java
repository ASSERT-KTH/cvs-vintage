package org.tigris.scarab.baseom;

// JDK classes
import java.util.*;

// Turbine classes
import org.apache.turbine.om.BaseObject;
import org.apache.turbine.om.peer.BasePeer;
import org.tigris.scarab.baseom.peer.*;
import org.apache.turbine.util.db.Criteria;
import org.apache.turbine.util.ObjectUtils;
import org.apache.turbine.util.StringUtils;
import org.apache.turbine.util.ParameterParser;
import org.apache.turbine.util.Log;
import org.apache.turbine.util.db.pool.DBConnection;

/** This class was autogenerated by GenerateMapBuilder on: Mon Jan 08 11:17:17 PST 2001 */
public class ScarabRModuleUserRole extends BaseObject
{
    /** the value for the module_id field */
    private int module_id;
    /** the value for the user_id field */
    private int user_id;
    /** the value for the role_id field */
    private int role_id;
    /** the value for the deleted field */
    private boolean deleted;


    /**
     * Get the ModuleId
     * @return int
     */
     public int getModuleId()
     {
          return module_id;
     }

            
    /**
     * Set the value of ModuleId
     */
     public void setModuleId(int v ) throws Exception
     {
                  if ( aScarabRModuleUser != null && !aScarabRModuleUser.isNew())
        {
            throw new Exception("Can't set a foreign key directly after an "
                + " association is already made based on saved data.");
        }
  
  

            if (this.module_id != v)
           {
              this.module_id = v;
              setModified(true);
          }
     }
    /**
     * Get the UserId
     * @return int
     */
     public int getUserId()
     {
          return user_id;
     }

            
    /**
     * Set the value of UserId
     */
     public void setUserId(int v ) throws Exception
     {
                  if ( aScarabRModuleUser != null && !aScarabRModuleUser.isNew())
        {
            throw new Exception("Can't set a foreign key directly after an "
                + " association is already made based on saved data.");
        }
  
  

            if (this.user_id != v)
           {
              this.user_id = v;
              setModified(true);
          }
     }
    /**
     * Get the RoleId
     * @return int
     */
     public int getRoleId()
     {
          return role_id;
     }

        
    /**
     * Set the value of RoleId
     */
     public void setRoleId(int v ) 
     {
  
  

            if (this.role_id != v)
           {
              this.role_id = v;
              setModified(true);
          }
     }
    /**
     * Get the Deleted
     * @return boolean
     */
     public boolean getDeleted()
     {
          return deleted;
     }

        
    /**
     * Set the value of Deleted
     */
     public void setDeleted(boolean v ) 
     {
  
  

           if (this.deleted != v)
           {
              this.deleted = v;
              setModified(true);
          }
     }

 
 
   
                    
   
   
    /**
     * Declares an association between this object and a ScarabRModuleUser object
     *
     * @param ScarabRModuleUser v
     */
    private ScarabRModuleUser aScarabRModuleUser;
    public void setScarabRModuleUser(ScarabRModuleUser v) throws Exception
    {
        aScarabRModuleUser = null;
           setModuleId(v.getModuleId());
           setUserId(v.getUserId());
           aScarabRModuleUser = v;
    }

                                
    public ScarabRModuleUser getScarabRModuleUser() throws Exception
    {
        if ( aScarabRModuleUser==null && (this.module_id>0 && this.user_id>0) )
        {
            aScarabRModuleUser = ScarabRModuleUserPeer.retrieveByPK(this.module_id, this.user_id);
            // The following can be used instead of the line above to
            // guarantee the related object contains a reference
            // to this object, but this level of coupling 
            // may be undesirable in many circumstances.
            // As it can lead to a db query with many results that may
            // never be used.  
            // ScarabRModuleUser obj = ScarabRModuleUserPeer.retrieveByPK(this.module_id, this.user_id);
            // obj.addScarabRModuleUserRoles(this);
        }
        return aScarabRModuleUser;
    }

    
        
    


    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     */
    public void save() throws Exception
    {
         DBConnection dbCon = null;
        try
        {
            dbCon = BasePeer.beginTransaction(
                ScarabRModuleUserRolePeer.getMapBuilder()
                .getDatabaseMap().getName());
            save(dbCon);
        }
        catch(Exception e)
        {
            BasePeer.rollBackTransaction(dbCon);
            throw e;
        }
        BasePeer.commitTransaction(dbCon);

     }

      // flag to prevent endless save loop, if this object is referenced
    // by another object which falls in this transaction.
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     */
    public void save(DBConnection dbCon) throws Exception
    {
        if (!alreadyInSave)
      {
        alreadyInSave = true;
          if (isModified())
        {
            if (isNew())
            {
                ScarabRModuleUserRolePeer.doInsert(this, dbCon);
            }
            else
            {
                ScarabRModuleUserRolePeer.doUpdate(this, dbCon);
                setNew(false);
            }
        }

              alreadyInSave = false;
      }
      }

                                                            
    /** 
     * Set the Id using pk values.
     *
     * @param int module_id
     * @param int user_id
     * @param int role_id
     */
    public void setPrimaryKey(
                      int module_id
                                      , int user_id
                                      , int role_id
                                         ) throws Exception
    {
                     setModuleId(module_id);
                             setUserId(user_id);
                             setRoleId(role_id);
                            }


    /** 
     * Set the Id using a : separated String of pk values.
     */
    public void setPrimaryKey(Object id) throws Exception
    {
        StringTokenizer st = new StringTokenizer(id.toString(), ":");
                     setModuleId( Integer.parseInt(st.nextToken()) );
                              setUserId( Integer.parseInt(st.nextToken()) );
                              setRoleId( Integer.parseInt(st.nextToken()) );
                  }


    /** 
     * returns an id that differentiates this object from others
     * of its class.
     */
    public Object getPrimaryKey() 
    {
        return ""
                      + getModuleId()
                                      + ":"  + getUserId()
                                      + ":"  + getRoleId()
                                         ;
    } 

    /** 
     * returns an id that can be used to specify this object in
     * a query string.
     */
    public String getQueryOID() 
    {
        return "ScarabRModuleUserRole[" + getPrimaryKey() + "]";
    }

    public static Vector getScarabRModuleUserRoles(ParameterParser pp)
        throws Exception
    {
        Vector v = new Vector();
        // look for classname in keys
        Enumeration keys = pp.keys();
        while ( keys.hasMoreElements() )
        {
            String ppKey = (String)keys.nextElement();
            String[] parsedString = StringUtils.parseObjectKey(ppKey);
            if ("ScarabRModuleUserRole".equalsIgnoreCase(
                     parsedString[StringUtils.PPKEY_CLASSNAME]))
            {
                ScarabRModuleUserRole obj = null;
                if (parsedString[StringUtils.PPKEY_ID].equals("PK") )
                {
                    obj = new ScarabRModuleUserRole();
                    v.addElement(obj);
                    StringTokenizer st = new StringTokenizer(
                        parsedString[StringUtils.PPKEY_PROPERTY], ":");
  
                          obj.setModuleId(Integer.parseInt(st.nextToken()));
        
                          obj.setUserId(Integer.parseInt(st.nextToken()));
        
                          obj.setRoleId(Integer.parseInt(st.nextToken()));
                        }
                else
                {
                    StringTokenizer st = new StringTokenizer(
                        parsedString[StringUtils.PPKEY_ID], ":");
  
                          int module_id = Integer.parseInt(st.nextToken());
        
                          int user_id = Integer.parseInt(st.nextToken());
        
                          int role_id = Integer.parseInt(st.nextToken());
        

                                  
                    for ( int i=0; i<v.size(); i++) 
                    {
                        ScarabRModuleUserRole tempObj = 
                            (ScarabRModuleUserRole)v.get(i);
                        if ( module_id == tempObj.getModuleId() && user_id == tempObj.getUserId() && role_id == tempObj.getRoleId() ) 
                        {
                            obj = tempObj;
                            break;
                        }
                    }
                    if ( obj == null ) 
                    {
                        obj = new ScarabRModuleUserRole();
                        obj.setModuleId(module_id);
                        obj.setUserId(user_id);
                        obj.setRoleId(role_id);
                        v.addElement(obj);
                    }
                }
                String property = parsedString[StringUtils.PPKEY_PROPERTY];

                                if ( property.equalsIgnoreCase("Deleted") ) 
                {
                    obj.setDeleted(pp.getBoolean(ppKey));
                }
                }
        }
        return v;
    }

    /**
     * Makes a copy of this object.  
     * It creates a new object filling in the simple attributes.
      * It then fills all the association collections and sets the
     * related objects to isNew=true.
      */
    public ScarabRModuleUserRole copy() throws Exception
    {
        ScarabRModuleUserRole copyObj = new ScarabRModuleUserRole();
         copyObj.setModuleId(module_id);
         copyObj.setUserId(user_id);
         copyObj.setRoleId(role_id);
         copyObj.setDeleted(deleted);
 
  
                        
        copyObj.setModuleId(NEW_ID);
                              
        copyObj.setUserId(NEW_ID);
                              
        copyObj.setRoleId(NEW_ID);
                  return copyObj;
    }

}
